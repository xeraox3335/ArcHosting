{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Arc Control (Electron)",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron",
      "windows": {
        "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron.cmd"
      },
      "args": [
        "${workspaceFolder}/main.js"
      ],
      "console": "integratedTerminal",
      "outputCapture": "std"
    }
  ]
}
